@*@model V2.Models.Chanson

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Titre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Commentaire)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateCreation)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Duree)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Prix)
        </th>


        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        if (item.Visible == true || 1 == 1)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Commentaire)
                </td>

                <td>
                    @Html.DisplayFor(modelItem => item.Duree)
                </td>
                <td>
                    @if (item.Prix >= 0)
                    {
                        @Html.DisplayFor(modelItem => item.Prix)
                    }
                    else
                    {
                        <p>-----</p>
                    }
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", "Versions", new { id = item.VersionID }, null) |
                    @Html.ActionLink("Details", "Details", "Versions", new { id = item.VersionID }, null) |
                    @Html.ActionLink("Delete", "Delete", "Versions", new { id = item.VersionID }, null)
                </td>
            </tr>
        }
    }

</table>*@
