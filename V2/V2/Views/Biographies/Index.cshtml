@model IEnumerable<V2.Models.Biographie>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
@if (Request.IsAuthenticated)
{ 
<p>
    @Html.ActionLink("Create New", "Create")
</p>
}
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Biographie1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateDernierChangement)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Artiste.Nom)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Biographie1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateDernierChangement)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Artiste.Nom)
        </td>
@if (Request.IsAuthenticated)
{ 
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BiographieID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BiographieID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BiographieID })
        </td>
}
    </tr>
}

</table>
